import { NgxCacheIfDirective } from './ngx-cache-if.directive';

describe('NgxCacheIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCacheIfDirective();
    expect(directive).toBeTruthy();
  });
});
